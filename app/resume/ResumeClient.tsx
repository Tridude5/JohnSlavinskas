"use client";

// Do NOT change your resume content. Simply paste your existing markup
// between the marked lines below. We intentionally use a Fragment so
// we don't introduce any extra wrappers that could alter layout/spacing.

export default function ResumeClient() {
  return (
    <>
      {/* ==== PASTE YOUR EXISTING RESUME MARKUP BELOW THIS LINE ==== */}

      {/* (example)
      <main className="mx-auto max-w-3xl px-4 py-12">
        <h1 className="text-3xl font-semibold">Resume</h1>
        ...the rest of your current resume content...
      </main>
      */}

      {/* ==== PASTE YOUR EXISTING RESUME MARKUP ABOVE THIS LINE ==== */}
    </>
  );
}
